import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#E0262D',
    },
    secondary: {
      main: '#7826E0',
    },
  },
});

export default theme;
